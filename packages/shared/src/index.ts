export type WallSummary = {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
  photoUrl?: string;
};

export const CANVAS_MAX_SIZE = 512;
export const DEFAULT_CANVAS_SIZE = 128;
